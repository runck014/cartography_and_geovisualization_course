import {
  r
} from "./chunk-DPVSO6BH.js";

// node_modules/@arcgis/core/views/webgl/ShaderCompiler.js
var e = class {
  constructor(e3) {
    this._readFile = e3;
  }
  resolveIncludes(e3) {
    return this._resolve(e3);
  }
  _resolve(e3, t = /* @__PURE__ */ new Map()) {
    if (t.has(e3)) return t.get(e3);
    const r2 = this._read(e3);
    if (!r2) throw new Error(`cannot find shader file ${e3}`);
    const s = /^[^\S\n]*#include\s+<(\S+)>[^\S\n]?/gm;
    let n = s.exec(r2);
    const l = [];
    for (; null != n; ) l.push({ path: n[1], start: n.index, length: n[0].length }), n = s.exec(r2);
    let a = 0, h = "";
    return l.forEach((e4) => {
      h += r2.slice(a, e4.start), h += t.has(e4.path) ? "" : this._resolve(e4.path, t), a = e4.start + e4.length;
    }), h += r2.slice(a), t.set(e3, h), h;
  }
  _read(e3) {
    return this._readFile(e3);
  }
};

// node_modules/@arcgis/core/views/webgl/ProgramTemplate.js
function e2(e3, t, a = "") {
  return new r(e3, a + t.shaders.vertexShader, a + t.shaders.fragmentShader, t.attributes);
}

export {
  e,
  e2
};
//# sourceMappingURL=chunk-FKL5AVF4.js.map
